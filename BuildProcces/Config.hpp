#pragma once

// Declarations var // extern - they exist somewhere, but not here
constexpr float JumpHeight = 50.0f; //constexpr - one definition of this variable and thats it, no possibility to change it - it tells to compiler that
inline float JumpDuration = 0.5f;